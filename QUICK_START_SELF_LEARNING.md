# ⚡ Быстрый старт - Самообучение и Автосинхронизация

## ✅ Что уже сделано:

### 🆕 Новые файлы:

1. **`src/models/dialog_rating.py`** - Модель для хранения рейтингов
2. **`src/auto_sync_drive.py`** - Автосинхронизация Google Drive
3. **`src/self_learning.py`** - Модуль самообучения
4. **`src/handlers/rating.py`** - Обработчик рейтингов
5. **`scripts/manual_sync_drive.py`** - Ручная синхронизация
6. **`scripts/manual_learning.py`** - Ручное обучение

### 📝 Изменения в существующих файлах:

- `src/main.py` - Добавлены фоновые задачи
- `src/bot.py` - Добавлен rating_router
- `src/handlers/__init__.py` - Экспорт rating_router
- `src/handlers/chat.py` - Запрос рейтинга каждый 3-й ответ

### 🗄️ База данных:

- Таблица `dialog_ratings` - **✅ СОЗДАНА**

---

## 🚀 Как это работает прямо сейчас:

### 1. Рейтинги от пользователей

```
Пользователь общается с ботом → 
Каждый 3-й ответ бот спрашивает:
┌────────────────────────────────────┐
│ 📊 Насколько полезен был мой ответ?│
│                                    │
│ ⭐ ⭐⭐ ⭐⭐⭐ ⭐⭐⭐⭐ ⭐⭐⭐⭐⭐      │
│         [Пропустить]                │
└────────────────────────────────────┘
```

### 2. Самообучение (каждые 2 часа)

```bash
# Автоматически:
- Выбирает диалоги с рейтингом ≥4 ⭐
- Форматирует как документ
- Создает эмбеддинг через OpenAI
- Добавляет в таблицу documents
- Помечает как обработанные
```

### 3. Google Drive синхронизация (каждый час, если настроено)

```bash
# Если GOOGLE_DRIVE_FOLDER_ID указан в .env:
- Проверяет новые файлы в папке
- Скачивает и извлекает текст
- Создает эмбеддинги
- Добавляет в documents
```

---

## 🧪 Тестирование

### Проверка 1: Рейтинги работают?

```bash
# 1. Откройте бота в Telegram
# 2. Выберите "Индивидуальная консультация"
# 3. Напишите 3 сообщения
# 4. После 3-го ответа должен появиться запрос рейтинга:
#    "📊 Насколько полезен был мой ответ?"

# 5. Проверить в БД:
docker exec maxcapital_db psql -U maxcapital -d maxcapital_bot -c \
  "SELECT id, user_id, rating, service, created_at FROM dialog_ratings ORDER BY created_at DESC LIMIT 5;"
```

### Проверка 2: Самообучение работает?

```bash
# Запустить вручную:
docker exec maxcapital_bot python scripts/manual_learning.py

# Ожидаемый вывод:
# 📊 Current Statistics:
# Total ratings: X
# Already learned: Y
# Pending to learn: Z
# Avg rating (30d): 4.2
#
# ✅ Self-learning completed!
# 🧠 Learned: Z dialogs
```

### Проверка 3: Drive синхронизация работает?

```bash
# Только если настроен Google Drive!

# Запустить вручную:
docker exec maxcapital_bot python scripts/manual_sync_drive.py

# Ожидаемый вывод:
# ✅ Drive sync completed!
# 📥 Synced: X
# ⏭️  Skipped: Y
```

---

## 📊 Мониторинг

### Логи фоновых задач

```bash
# Смотреть все логи в реальном времени
docker-compose logs -f bot | grep -E "learning|drive|rating"

# Только самообучение
docker-compose logs bot | grep learning

# Только Drive
docker-compose logs bot | grep drive

# Только рейтинги
docker-compose logs bot | grep rating
```

### SQL проверки

```sql
-- Сколько рейтингов собрано?
SELECT COUNT(*) FROM dialog_ratings;

-- Распределение по звездам
SELECT rating, COUNT(*) as count 
FROM dialog_ratings 
GROUP BY rating 
ORDER BY rating DESC;

-- Средний рейтинг
SELECT AVG(rating)::numeric(10,2) as avg_rating 
FROM dialog_ratings;

-- Сколько уже добавлено в базу знаний?
SELECT COUNT(*) FROM dialog_ratings 
WHERE is_added_to_knowledge = true;

-- Ждут добавления (рейтинг ≥4)
SELECT COUNT(*) FROM dialog_ratings 
WHERE rating >= 4 AND is_added_to_knowledge = false;
```

---

## ⚙️ Настройка параметров

### Частота запроса рейтинга

**Файл:** `src/handlers/chat.py`, строка ~156

```python
if conversation_length % 3 == 0:  # Каждый 3-й ответ
```

Изменить на:
- `% 1` - после каждого ответа (навязчиво!)
- `% 5` - каждый 5-й ответ
- `% 10` - каждый 10-й ответ

### Интервал самообучения

**Файл:** `src/self_learning.py`, строка ~17

```python
self.learning_interval_minutes = 120  # 2 часа
```

Изменить на:
- `30` - каждые 30 минут
- `60` - каждый час
- `1440` - раз в день

### Минимальный рейтинг для обучения

**Файл:** `src/self_learning.py`, строка ~16

```python
self.min_rating_threshold = 4  # Только 4-5 звезд
```

Изменить на:
- `3` - включить 3+ звезды
- `5` - только 5 звезд

### Интервал Drive синхронизации

**Файл:** `src/auto_sync_drive.py`, строка ~24

```python
self.sync_interval_minutes = 60  # 1 час
```

---

## 🔧 Troubleshooting

### Проблема: Рейтинги не появляются

**Решение:**
```bash
# 1. Проверить логи
docker-compose logs bot | grep rating

# 2. Убедиться, что прошло 3 сообщения
# 3. Проверить состояние conversation_history
docker exec maxcapital_db psql -U maxcapital -d maxcapital_bot -c \
  "SELECT user_id, jsonb_array_length(conversation_history) as msg_count FROM user_memory;"
```

### Проблема: Самообучение не запускается

**Решение:**
```bash
# 1. Проверить логи
docker-compose logs bot | grep "self_learning_started"

# 2. Если не видите - перезапустить
docker-compose restart bot

# 3. Запустить вручную для теста
docker exec maxcapital_bot python scripts/manual_learning.py
```

### Проблема: Drive синхронизация не работает

**Решение:**
```bash
# 1. Проверить .env
docker exec maxcapital_bot printenv GOOGLE_DRIVE_FOLDER_ID

# 2. Проверить credentials.json
docker exec maxcapital_bot ls -la /app/credentials.json

# 3. Проверить права на Google Drive
# Убедитесь, что service account email имеет доступ к папке

# 4. Запустить вручную
docker exec maxcapital_bot python scripts/manual_sync_drive.py
```

---

## 📈 Ожидаемые результаты

### После 1 дня:
- ✅ 10-20 рейтингов собрано
- ✅ 3-5 диалогов добавлено в базу знаний
- ✅ Средний рейтинг виден

### После 1 недели:
- ✅ 50-100 рейтингов
- ✅ 15-30 диалогов в базе знаний
- ✅ RAG начинает находить примеры из реальных диалогов

### После 1 месяца:
- ✅ 200-500 рейтингов
- ✅ 50-150 диалогов в базе знаний
- ✅ Заметное улучшение качества ответов
- ✅ Средний рейтинг растет

---

## 🎯 Следующие шаги

### 1. Добавить статистику в админ-панель

**TODO:** Добавить endpoint в `src/web/api.py`:

```python
@app.get("/api/learning-stats")
async def get_learning_stats(
    request: Request,
    session: AsyncSession = Depends(get_db_session),
    _: None = Depends(require_auth)
):
    from src.self_learning import self_learning
    stats = await self_learning.get_learning_statistics(session)
    return stats
```

### 2. Добавить дашборд в админку

**TODO:** В `src/web/templates/index.html` добавить:

```html
<div id="learning-stats">
    <h3>Самообучение</h3>
    <div>Всего рейтингов: <span id="total-ratings">0</span></div>
    <div>Обучено диалогов: <span id="learned-dialogs">0</span></div>
    <div>Средний рейтинг: <span id="avg-rating">0</span></div>
</div>
```

### 3. Email уведомления о низких рейтингах

**TODO:** При рейтинге 1-2 звезды отправлять email менеджерам.

---

## ✅ Готово!

Бот теперь:
- ✅ Собирает рейтинги
- ✅ Учится на лучших диалогах
- ✅ Синхронизирует Google Drive (если настроено)
- ✅ Улучшается автоматически 24/7

**Протестируйте прямо сейчас:**
1. Откройте бота в Telegram
2. Выберите "Индивидуальная консультация"
3. Напишите 3 сообщения
4. Оцените ответ 5 звездами ⭐⭐⭐⭐⭐
5. Через 2 часа этот диалог появится в базе знаний!

---

*Версия: 1.0*  
*Дата: 2 декабря 2025*





